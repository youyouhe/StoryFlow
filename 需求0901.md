AI视频生成模块产品需求指南
基于 graybox 白模工作流 + 多模态参考图体系的完整产品设计方案

适用对象：剧本平台的 AI 视频生成模块
版本：v1.0
更新日期：2026-09-01

一、模块定位与核心理念
1.1 模块定位
将剧本平台的 graybox 3D 预演数据，转化为高质量 AI 视频的一站式渲染工作台。

1.2 核心理念
理念	说明
骨架-血肉分离	白模提供运动骨架，参考图提供视觉血肉，各司其职
多源参考融合	文生图、网络检索、视频抽帧、平台图库四源合一
确定性产出	同样的输入 → 同样的输出，支持版本迭代和 A/B 测试
镜头即指令	每个 shot 的摄像机参数直接映射为视频生成的控制信号
1.3 用户旅程
text
用户完成剧本创作 → 生成 graybox 3D预演 → 进入AI渲染工作台
     ↓
步骤1：选择输出模型（Seedance 2.5 / 深度图模型 / 其他）
     ↓
步骤2：准备参考图资产（来源：文生图/网络检索/视频抽帧/平台图库）
     ↓
步骤3：资产分配与白模绑定（哪个角色对应哪张图）
     ↓
步骤4：编写/生成提示词（系统辅助生成，含@引用语法）
     ↓
步骤5：发起生成任务（排队/进度/结果预览）
     ↓
步骤6：结果对比与迭代（版本管理 + 参数调优）
二、模型接入层
2.1 支持的模型矩阵
模型	控制方式	输入格式	输出	接入状态
Seedance 2.5	白模视频 + 参考图 + 提示词	MP4 + JPEG + Text	MP4 (≤30s/1080p)	推荐
LTX-2 19B ControlNet	深度图 + 提示词	PNG(深度图) + Text	MP4	可选
Wan2.1-VACE	深度图/线稿/姿态 + 提示词	PNG + Text	MP4	可选
Wan2.2-Fun-Control	Canny/深度/姿态 + 提示词	PNG + Text	MP4	可选
未来扩展	自定义	—	—	预留接口
2.2 模型选择决策树（用户侧引导）
text
用户需求评估
    │
    ├── 需要精确复现复杂运镜 + 角色走位？
    │       └── 是 → 推荐 Seedance 2.5（白模工作流）
    │
    ├── 已有固定场景，只想换风格/材质？
    │       └── 是 → 推荐 深度图模型（LTX-2 / Wan2.1-VACE）
    │
    ├── 需要实时预览或快速迭代？
    │       └── 是 → 推荐 轻量级模型（待定）
    │
    └── 默认 → 推荐 Seedance 2.5（最成熟的综合方案）
2.3 输出规格控制
参数	Seedance 2.5	深度图模型
最大时长	30秒	视模型而定（通常5-10秒）
最高分辨率	1080p	1080p
帧率	24fps（固定）	24/30fps（可配置）
输出格式	MP4 (H.264)	MP4 (H.264)
三、参考图资产管理系统
3.1 四大来源入口
用户可通过以下四种方式获取参考图，所有来源的资产统一进入参考图资产库管理：

来源	入口	说明
① 文生图生成	平台内置	用户输入文本描述，调用平台文生图能力生成参考图；支持扩展提示词（主体/环境/光影/色彩/构图7大维度）
② 网络检索	搜索框	用户输入关键词，检索并导入公开版权图库（如 Unsplash、Pexels）；或上传URL导入外部图片
③ 视频抽帧	视频上传	用户上传参考视频 → 系统自动按时间间隔（如每0.5秒）抽取关键帧 → 用户筛选导入
④ 平台图库	平台素材库	复用项目中已有的图片素材；支持从其他项目/场景导入
3.2 资产查看器：参考图四维管理
所有参考图资产以列表/网格视图呈现，每张图需标注以下四个维度的标签，便于后续检索和分配：

维度	可选标签	说明
身份类	角色-主角 / 角色-配角 / 角色-群演 / 道具-关键 / 道具-环境	标注内容是谁或什么
风格类	构图参考 / 光照参考 / 色彩参考 / 材质参考 / 氛围参考	标注图传递的风格信息类型
用途类	角色锁定 / 场景锁定 / 道具锁定 / 姿态参考 / 运镜参考	标注生成时如何使用这张图
来源类	文生图 / 网络检索 / 视频抽帧 / 平台图库	追溯资产来源，便于版权管理
交互要求：

用户可通过任意维度组合筛选参考图

支持批量标注（选择多张图统一打标）

支持按项目/场景/角色分组管理

3.3 参考图预览与拆解工具
功能：上传一张混合内容的图片（如电影剧照同时含人物、道具、环境），AI自动识别并拆解为独立资产：

人物/生物的多角度参考表 → 拆出角色图

道具/物体的独立参考图 → 拆出道具图

场景构图参考 → 拆出环境图

风格DNA → 自动提取色彩、光影、材质、氛围特征

输出：拆解后的独立图片分别入库，自动打标。

四、资产分配与绑定
4.1 核心交互：白模 → 参考图 映射
用户需要将参考图与 graybox 白模中的元素对应起来。在 Seedance 2.5 中，这是通过提示词中的 @引用语法 实现的。平台应提供可视化绑定界面，自动生成符合语法的提示词。

交互设计：

text
┌─────────────────────────────────────────────────────────┐
│  白模元素列表          ↔   参考图资产库              │
│                                                         │
│  ● 蓝色胶囊体 [角色]  →   [图1] 林枫（主角）  已绑定 │
│  ● 红色胶囊体 [角色]  →   [图2] 莫长老        已绑定 │
│  ● 灰色方块 [道具]    →   [图3] 古镜          已绑定 │
│  ○ 绿色方块 [环境]    →   [待分配]            未绑定 │
│                                                         │
│  [绑定]  [解除绑定]  [AI推荐绑定]                     │
└─────────────────────────────────────────────────────────┘
4.2 AI 推荐绑定
用户点击后，系统根据以下逻辑自动推荐映射关系：

白模元素的 label 与参考图文件名/标签的文本相似度匹配

若白模有 role 标注（如 role: "主角"），优先匹配对应身份类参考图

推荐结果展示为候选列表，用户确认后一键绑定

4.3 绑定数据 → 提示词生成
绑定的映射关系会自动翻译为提示词中的 @ 引用语法：

text
将其中 蓝色胶囊体 映射为 @Image1（林枫）
将其中 红色胶囊体 映射为 @Image2（莫长老）
将其中 灰色方块 映射为 @Image3（古镜）
五、提示词工程系统
5.1 提示词结构模板
参考 Seedance 2.5 官方最佳实践，系统应提供以下模板并支持用户自定义编辑：

text
【第一部分：白模引用 + 映射分配】
参考 @视频1 的运镜、动作路径、角色站位与镜头节奏。
将其中 [绑定映射内容]。
严格保持白模视频的镜头顺序、机位变化、运动方式和节奏，
不改变镜头结构，不新增镜头，不改变主体运动逻辑。

【第二部分：按时间戳的动作/光影描述】
0-Xs：[描述开头画面、角色动作、光影变化]
X-Ys：[描述中间过程]
Y-Zs：[描述结尾收束]

【第三部分：风格锁定 + 全局约束】
整体风格：[风格关键词]
角色外形与各阶段的关键帧保持一致，不要改变角色形象。
全程保持角色身份、比例、站位逻辑和动作连续；
不保留白模材质、轨迹线、坐标线或相机锥体。
5.2 智能提示词助手
功能	说明
模板库	预置多种场景模板（动作/对话/风景/特效），一键套用
时间轴辅助	用户点击graybox的镜头时间线，系统自动填充对应时段的描述占位符
词库推荐	根据选择的风格关键词，推荐专业视觉描述词（如"赛博朋克"→霓虹/雨夜/全息投影）
长度检查	自动检测提示词长度是否在模型推荐范围内（通常≤500字符）
@引用校验	检查所有引用的图片/视频是否已上传，检查映射关系是否完整
5.3 提示词版本管理
功能	说明
版本历史	每次生成的提示词自动保存，支持回溯和对比
A/B测试	同一graybox支持生成多个提示词版本，分别发起任务，结果并列对比
模板保存	用户可将常用提示词保存为个人模板
六、生成任务管理
6.1 任务工作流
text
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  提交任务   │ → │  排队中     │ → │  生成中     │ → │  已完成     │ │  失败/重试  │
│  (配置确认) │    │  (等待资源) │    │  (进度条)   │ │  (预览/下载) │ │  (错误诊断) │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
6.2 批量生成
功能	说明
多镜头批量	一个graybox包含多个shot，支持逐个提交或全部提交
多版本对比	同一任务自动生成3-5个变体，供用户选择最佳结果
队列管理	显示当前排队位置、预计等待时间、剩余生成配额
并发控制	用户可设置最大并发任务数（根据API配额限制）
6.3 结果管理
功能	说明
结果预览	生成完成后自动播放预览，支持静音/循环/全屏
帧级检查	提供逐帧查看模式，方便检查角色一致性、镜头连续性
版本对比	同一任务的不同变体并列显示，支持点赞/标记/评分
导出下载	支持下载原视频（MP4）和关键帧截图
一键重生成	不满意时可基于相同配置快速重跑
七、白模预处理与验证
7.1 渲染前自动体检
在用户发起渲染任务前，系统自动对 graybox 数据进行几何逻辑检查，在用户提交前拦截常见错误，避免生成失败或画面异常。

检查项	说明	错误示例
相机-主体碰撞	检查摄像机位置与主体位置是否重叠	相机坐标与角色坐标完全重合
主体出画	检查主体是否在摄像机视野内	角色站在摄像机背后/侧方
景别覆盖	检查远景/中景/特写的覆盖范围是否合理	所有镜头都是同一景别
组队半径	多人场景中检查角色间距是否超出机位覆盖	站位太分散，镜头装不下所有人
运动路径完整性	检查 movement.path 的坐标是否连续、镜头是否平滑过渡	路径中存在跳点/断裂
角色朝向一致性	检查 facing 值与移动方向是否匹配	角色朝南走但面朝北
道具位置合理性	检查道具是否被角色遮挡或放置于不可达区域	关键道具放在角色背后
镜头运动时长	检查 movement.duration 是否合理	0秒或超过模型最大时长限制
交互设计：

体检结果以检查清单形式展示，每项标记 ✅/⚠️/❌

⚠️ 警告项提示用户确认，❌ 错误项阻止提交

点击错误项可跳转到对应的3D预览场景，高亮问题区域

7.2 白模视频导出（干净渲染）
在用户提交生成任务时，系统自动执行以下流程：

text
步骤1：根据 graybox 数据加载场景
步骤2：隐藏所有辅助元素（网格/坐标轴/轨迹线/标签/摄像机锥体）
步骤3：按 camera.movement.path 播放摄像机运动
步骤4：录制 Canvas 画面
步骤5：导出 MP4 视频
步骤6：自动上传至模型API
输出规格：

格式：MP4 / H.264

帧率：24fps

分辨率：1080p（固定）

时长：自动匹配 movement.duration

7.3 渲染后验证
在AI生成完成后，系统自动/半自动进行质量验证：

验证项	说明
首尾帧一致性	检查首尾帧的角色、场景、构图是否符合预期
景别匹配	抽帧检查每个镜头的景别是否与设计一致
主体朝向验证	跟拍段主体应为背面，前置倒退段应为正面
切点机位变化	镜头切换点前后是否真的换了机位
参考图匹配度	角色外观是否与参考图一致（用CLIP等模型打分）
验证结果在任务详情页展示，标记 🟢/🟡/🔴，供用户快速判断是否需要重跑。

八、用户体验流程（分步设计）
8.1 入口：从graybox到AI渲染
用户完成剧本创作后，在场景/镜头卡片上看到 "AI渲染" 按钮。点击后进入渲染工作台，当前 graybox 数据自动载入。

8.2 步骤1：选择模型
text
┌─────────────────────────────────────────────────────────┐
│  选择AI模型                                            │
│                                                         │
│  ○ Seedance 2.5  [推荐]  白模视频工作流，精确控制运镜 │
│  ○ LTX-2 ControlNet      深度图控制，适合风格转换    │
│  ○ Wan2.1-VACE           多控制信号，灵活度最高      │
│                                                         │
│  [下一步]                                              │
└─────────────────────────────────────────────────────────┘
8.3 步骤2：准备参考图
text
┌─────────────────────────────────────────────────────────┐
│  参考图资产库                                          │
│                                                         │
│  [文生图]  [网络检索]  [视频抽帧]  [平台图库]          │
│                                                         │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ 图1   │ │ 图2   │ │ 图3   │ │ 图4   │ │ 图5   │       │
│  │ 角色  │ │ 角色  │ │ 场景  │ │ 道具  │ │ 环境  │       │
│  │ 主角  │ │ 配角  │ │ 构图  │ │ 古镜  │ │ 氛围  │       │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘       │
│                                                         │
│  已导入 5 张参考图                                     │
│                                                         │
│  [下一步]                                              │
└─────────────────────────────────────────────────────────┘
8.4 步骤3：绑定映射
text
┌─────────────────────────────────────────────────────────┐
│  白模元素 → 参考图 绑定                                │
│                                                         │
│  白模元素           参考图         状态                │
│  ● 蓝色胶囊体  →   [图1] 林枫   ✅ 已绑定            │
│  ● 红色胶囊体  →   [图2] 莫长老 ✅ 已绑定            │
│  ● 灰色方块    →   [图3] 古镜   ✅ 已绑定            │
│  ○ 绿色方块    →   [待分配]     ⚠️ 建议绑定场景图    │
│                                                         │
│  [AI推荐绑定]  [全部清除]  [确认绑定]                 │
│                                                         │
│  [下一步]                                              │
└─────────────────────────────────────────────────────────┘
8.5 步骤4：提示词编辑
text
┌─────────────────────────────────────────────────────────┐
│  提示词编辑器                                          │
│                                                         │
│  [模板库 ▼]  [AI辅助生成]  [长度: 234/500]             │
│                                                         │
│  ┌─────────────────────────────────────────────────┐    │
│  │ 参考@视频1的运镜、动作路径、角色站位与镜头节奏。│    │
│  │ 将其中蓝色胶囊体映射为@Image1（林枫）；        │    │
│  │ 将其中红色胶囊体映射为@Image2（莫长老）。      │    │
│  │ 0-3s：林枫从跪地缓缓抬头，目光如炬...         │    │
│  │ 3-6s：莫长老拂袖怒喝，袖袍鼓荡...             │    │
│  │ 整体风格：写实仙侠，冷色调，庄严压抑。         │    │
│  │ 全程保持角色身份、比例、站位逻辑一致。         │    │
│  └─────────────────────────────────────────────────┘    │
│                                                         │
│  [提示词检查]  [保存模板]                              │
│                                                         │
│  [提交生成]                                            │
└─────────────────────────────────────────────────────────┘
8.6 步骤5：任务提交与进度
text
┌─────────────────────────────────────────────────────────┐
│  任务提交中...                                         │
│                                                         │
│  ✅ 白模体检通过                                       │
│  ✅ 白模视频导出中 (85%)                               │
│  ⏳ 提交给 Seedance 2.5...                            │
│  ⏳ 排队中 (位置 3/12)                                 │
│  ⏳ 生成中 ████████░░░ 67%                            │
│                                                         │
│  预计剩余时间：2分30秒                                 │
│                                                         │
│  [后台运行]  [取消任务]                                │
└─────────────────────────────────────────────────────────┘
8.7 步骤6：结果预览与迭代
text
┌─────────────────────────────────────────────────────────┐
│  生成完成！                                            │
│                                                         │
│  ┌─────────────────────────────────────────────────┐    │
│  │  ▶ 视频预览区域 (播放/暂停/循环/全屏)         │    │
│  │                                                 │    │
│  └─────────────────────────────────────────────────┘    │
│                                                         │
│  版本：v1.0.3  |  时长：8s  |  1080p  |  24fps       │
│                                                         │
│  [下载]  [重生成]  [标记满意]  [对比版本]              │
│                                                         │
│  质量检查：                                            │
│  🟢 首尾帧一致性  🟢 景别匹配  🟡 角色外观  🔴 光影   │
│                                                         │
│  [查看详情]  [基于此版本调整参数重新生成]              │
└─────────────────────────────────────────────────────────┘
九、数据模型扩展
9.1 新增实体
在现有剧本数据模型基础上，AI视频渲染模块需新增以下实体：

实体	说明	关键字段
ReferenceAsset	参考图资产	id, source(文生图/网络/抽帧/图库), url, tags(四维标签), projectId
RenderTask	生成任务	id, grayboxId, model(seedance/ltx-2/wan), status, prompt, resultUrl, createdAt
AssetBinding	绑定关系	id, taskId, blockId(白模元素id), assetId(参考图id), role(角色/道具/环境)
PromptTemplate	提示词模板	id, name, content, styleTags, userId
GenerationVersion	生成版本	id, taskId, version, resultUrl, score, isSelected
9.2 与现有数据的关系
text
剧本 (Script)
    └── 场景 (Scene)
        └── 镜头 (Shot) ←── graybox数据
            └── AI渲染任务 (RenderTask)
                ├── 参考图绑定 (AssetBinding) ←── ReferenceAsset
                ├── 提示词 (Prompt)
                └── 生成版本 (GenerationVersion) ←── 结果视频
十、技术实现建议
10.1 前端组件清单
组件	功能	对应章节
ModelSelector	模型选择与推荐	二、8.2
ReferenceAssetManager	参考图资产库（四源入口+四维标签）	三
AssetBindingPanel	白模-参考图绑定界面	四
PromptEditor	提示词编辑+智能辅助	五
TaskStatusDashboard	任务状态看板	六、8.6
ResultPreview	视频预览+质量验证	六、8.7
GrayboxHealthCheck	白模自动体检	七
10.2 后端服务架构
text
┌─────────────────────────────────────────────────────────────┐
│  API Gateway                                               │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
┌───────▼───────┐    ┌───────▼───────┐    ┌───────▼───────┐
│  资产服务      │    │  任务服务      │    │  模型网关      │
│  (参考图管理)  │    │  (任务调度)    │    │  (多模型适配)  │
└───────────────┘    └───────────────┘    └───────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                    ┌─────────▼─────────┐
                    │   白模渲染服务     │
                    │  (Three.js渲染)    │
                    └───────────────────┘
10.3 关键接口设计
typescript
// 提交生成任务
POST /api/v1/render/tasks
Request: {
  grayboxId: string;
  modelId: 'seedance-2.5' | 'ltx-2-controlnet' | 'wan-vace';
  bindings: Array<{ blockId: string; assetId: string; role: string }>;
  prompt: string;
  options?: { duration?: number; resolution?: '720p' | '1080p' };
}
Response: { taskId: string; status: 'pending' }

// 查询任务状态
GET /api/v1/render/tasks/:taskId
Response: {
  status: 'pending' | 'rendering' | 'completed' | 'failed';
  progress?: number;
  result?: { url: string; duration: number; resolution: string };
  error?: string;
}

// 健康检查
POST /api/v1/render/graybox/check
Request: { grayboxId: string }
Response: {
  passed: boolean;
  checks: Array<{ name: string; status: 'pass' | 'warn' | 'fail'; message?: string }>;
}
十一、部署与运营建议
11.1 API配额管理
项目	说明
模型配额	按用户/项目限制每日调用次数，避免超出API限额
并发控制	限制同时运行的任务数，防止API过载
队列优先级	付费用户/紧急任务可优先调度
11.2 成本控制
策略	说明
自动重试限制	同一任务失败后最多自动重试3次，超出后标记为需人工介入
预览缩略图	结果预览默认加载压缩版本，点击才加载原视频，降低带宽成本
缓存机制	相同输入（graybox + 参考图 + 提示词）的任务自动去重，复用已生成结果
配额预警	当月使用量达到配额的80%时触发预警通知
11.3 合规与版权
策略	说明
来源追溯	每张参考图标记来源（文生图/网络/抽帧/图库），便于版权核查
网络检索过滤	仅接入公开版权图库（如Unsplash、Pexels），过滤有版权风险的内容
生成内容标识	按照行业规范，在生成视频中添加AI生成标识（水印或元数据）
用户协议	用户上传素材时需确认拥有使用权
十二、优先级规划
Phase 1（MVP）
□ 接入 Seedance 2.5 API
□ 参考图资产管理（文生图 + 网络检索）
□ 白模-参考图绑定界面
□ 提示词编辑器（基础模板）
□ 白模自动体检（基础检查项）
□ 任务提交 + 状态查询
□ 结果预览 + 下载
Phase 2（增强）
□ 接入深度图模型（LTX-2 / Wan2.1-VACE）
□ 视频抽帧导入参考图
□ 平台图库复用
□ AI推荐绑定
□ 参考图拆解工具
□ 提示词智能助手
□ 渲染后验证（自动质量检查）
□ 批量生成 + 多版本对比
Phase 3（高级）
□ 多模态参考（视频+音频）
□ A/B测试框架
□ 个性化风格模型微调
□ 团队协作（共享资产库/模板）
附录A：参考图四维标签速查表
维度	可选值
身份类	角色-主角 / 角色-配角 / 角色-群演 / 道具-关键 / 道具-环境 / 场景-室内 / 场景-室外
风格类	构图参考 / 光照参考 / 色彩参考 / 材质参考 / 氛围参考 / 特效参考
用途类	角色锁定 / 场景锁定 / 道具锁定 / 姿态参考 / 运镜参考 / 风格参考
来源类	文生图 / 网络检索 / 视频抽帧 / 平台图库 / 用户上传
附录B：提示词模板库示例
模板名	适用场景	风格关键词
仙侠对峙	角色对峙/谈判场景	仙侠、肃杀、冷色调、古风
战斗爆发	动作/战斗场景	热血、快节奏、粒子特效、动态模糊
风景叙事	空镜/转场/环境	宏大、静谧、自然光、壮丽
情感对话	双人对话/情感戏	温暖、柔光、微表情、细腻
奇幻觉醒	突破/觉醒/变身	玄幻、发光、能量流动、震撼
附录C：白模体检错误码速查
错误码	说明	建议
E001	摄像机与主体位置重叠	调整 camera.position 坐标，确保距主体 ≥1单位
E002	主体在摄像机视野之外	检查 lookAt 是否指向主体，或调整主体位置
E003	运动路径中存在断点	检查 movement.path 坐标序列是否连续
E004	镜头时长超出模型限制	Seedance 2.5 最大30秒，超出需拆分
W001	所有镜头景别相同	建议混用远景/中景/特写增强叙事节奏
W002	多人场景站位半径过大	检查所有角色是否在摄像机视野覆盖范围内
W003	角色朝向与运动方向不匹配	跟拍时角色朝前走应面向运动方向
文档版本：v1.0
更新日期：2026-09-01
适用范围：剧本平台 AI 视频生成模块开发

本回答由 AI 生成，内容仅供参考，请仔细甄别
